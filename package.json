{
  "name": "sites-sobraep",
  "version": "1.0.0",
  "description": "Site institucional da SOBRAEP (Associação Brasileira de Eletrônica de Potência)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js",
    "seed-blob": "node --env-file-if-exists=.env scripts/seed-blob.mjs"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "@vercel/blob": "^2.6.1",
    "ejs": "^3.1.10",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "sharp": "^0.35.3"
  }
}
